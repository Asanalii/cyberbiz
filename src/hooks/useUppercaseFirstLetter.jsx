import React from 'react';

const useUppercaseFirstLetter = ({ text, sizeOfText, sizeOfFirstLetter }) => {
  const NewText = () => {
    return <p></p>;
  };
  return <div>useUppercaseFirstLetter</div>;
};

export default useUppercaseFirstLetter;
